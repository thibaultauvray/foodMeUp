<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /app/web

    <Directory /app/web>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
